# Local development examples only. Do not commit real credentials.
# Copy values into your environment or a local untracked .env file if your
# chosen launcher loads one.

A_LMI_MINIO_ACCESS_KEY=a_lmi_local
A_LMI_MINIO_SECRET_KEY=change-me-local-only
A_LMI_NEO4J_USERNAME=neo4j
A_LMI_NEO4J_PASSWORD=change-me-local-only
