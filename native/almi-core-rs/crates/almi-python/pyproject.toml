[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "almi-native"
version = "0.1.0"
requires-python = ">=3.11"
description = "Native Rust continuity bindings for A-LMI"

[tool.maturin]
bindings = "pyo3"
module-name = "almi_native"
